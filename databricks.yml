bundle:
  name: DABDemo


# resource:
#   #resources would be placed here.

include:
  - src/*.yml
  - src/env/*yml
  - resources/jobs/*.yml
  - resources/pipelines/*.yml
  - resources/schemas/*.yml
  - resources/dashboards/*.yml

sync:
  exclude:
    - resources/dashboards/*.lvdash.json

permissions:
  - group_name: de_group
    level: CAN_MANAGE



targets:
  dev:
    mode: development
    default: true
    workspace: 
      host: https://adb-3053813127396191.11.azuredatabricks.net
  qa:
    default: false
    workspace:
      host: 

